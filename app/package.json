{
  "name": "bmmro-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "TZ=UTC react-scripts test",
    "eject": "react-scripts eject",
    "format": "prettier --write 'src/' 'firestore/'",
    "format-check": "prettier --check 'src/' 'firestore/'",
    "test-firestore": "firebase emulators:exec --only firestore 'jest firestore/'"
  },
  "dependencies": {
    "@emotion/core": "^10.0.28",
    "@reach/menu-button": "^0.10.4",
    "@reach/router": "^1.3.3",
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.5.0",
    "date-fns": "^2.14.0",
    "firebase": "^7.14.6",
    "formik": "^2.1.4",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-scripts": "3.4.1"
  },
  "devDependencies": {
    "husky": "^4.2.5",
    "prettier": "^2.0.5",
    "@firebase/testing": "^0.15.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run format-check && npm test -- --watchAll=false"
    }
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
